import React from "react";
import ReactDOM from "react-dom";

import UnitCalculator from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <UnitCalculator />
  </React.StrictMode>,
  rootElement
);
